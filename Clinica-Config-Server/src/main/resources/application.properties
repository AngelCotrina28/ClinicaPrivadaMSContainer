spring.application.name=clinica-config-server
server.address=0.0.0.0
server.port=${PORT:8888}

spring.profiles.active=${CONFIG_PROFILE:native}
spring.cloud.config.server.native.search-locations=${CONFIG_REPO_URI:file:./config-repo}

management.endpoints.web.exposure.include=health,info
