# Clinica Config Server

Microservicio de infraestructura que centraliza configuraciones compartidas.

Corre localmente en:

```text
http://localhost:8888
```

Ejemplo de consulta:

```powershell
Invoke-RestMethod http://localhost:8888/clinica-gateway-service/default
```
